import { motion } from "framer-motion";
import Navbar from "../components/navbar";

export default function Gallery() {
	return (
		<div className="w-screen overflow-hidden h-[70vh] md:h-screen bg-colorThree mt-8">
			GALLERY
		</div>
	);
}
